[Global]
LOCALES = FR # Among EN and FR
FULLSCREEN = True

[Picture]
COUNTDOWN = 5
DCIM_DIRECTORY = ./DCIM
HYBRID_ZOOM = None # Run test/calibration_zoom.py to find the correct tupple to apply (zoom, offset_x, offset_y)
OVERLAY_INDEX = 0 # Set of overlays to use (See files `overlays/fullpage{index}.png` and `overlays/strip{index}.png`)